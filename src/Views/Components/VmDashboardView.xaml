<UserControl xmlns:properties="clr-namespace:ExHyperV.Properties" x:Class="ExHyperV.Views.VmDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:ExHyperV.Converters" 
             xmlns:tools="clr-namespace:ExHyperV.Tools"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="800">

    <UserControl.Resources>
        <DrawingBrush x:Key="FineGridBrush" Viewport="0,0,10,10" ViewportUnits="Absolute" TileMode="Tile">
            <DrawingBrush.Drawing>
                <GeometryDrawing>
                    <GeometryDrawing.Geometry>
                        <RectangleGeometry Rect="0,0,10,10"/>
                    </GeometryDrawing.Geometry>
                    <GeometryDrawing.Pen>
                        <Pen Brush="#15FFFFFF" Thickness="0.5"/>
                    </GeometryDrawing.Pen>
                </GeometryDrawing>
            </DrawingBrush.Drawing>
        </DrawingBrush>
        <converters:ScalePointsConverter x:Key="ScalePoints"/>
    </UserControl.Resources>

    <ScrollViewer Padding="24,0,24,24" VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
        <StackPanel>
            
            <ui:CardExpander Margin="0,0,0,8" IsExpanded="False">
                <ui:CardExpander.Header>
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xF58E;" RenderTransformOrigin="0.5,0.5" Margin="-5,0,12,0">
                                <ui:FontIcon.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform/>
                                        <SkewTransform/>
                                        <RotateTransform Angle="90"/>
                                        <TranslateTransform/>
                                    </TransformGroup>
                                </ui:FontIcon.RenderTransform>
                            </ui:FontIcon>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.Label_Processor}" FontSize="16" />
                                <TextBlock Text="{x:Static properties:Resources.Desc_CpuMonitoring}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{Binding SelectedVm.AverageUsage, StringFormat='{}{0:N1}'}" FontSize="20" FontWeight="Light" VerticalAlignment="Bottom" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Text="%" FontSize="14" Margin="2,0,0,3" VerticalAlignment="Bottom" Opacity="0.8"/>
                        </StackPanel>
                        <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToCpuSettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18"/>
                        </ui:Button>
                    </Grid>
                </ui:CardExpander.Header>

                <Grid Margin="-5">
                    <ItemsControl ItemsSource="{Binding SelectedVm.Cores}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <tools:StretchUniformGrid 
                                    Columns="{Binding SelectedVm.Columns}" 
                                    Rows="{Binding SelectedVm.Rows}" 
                                    />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="1" Height="80">
                                    <Border CornerRadius="4" BorderThickness="1" BorderBrush="#1A888888" Background="#08FFFFFF" ClipToBounds="True">
                                        <Grid>
                                            <Rectangle Fill="{StaticResource FineGridBrush}"/>
                                            <!-- 按实际像素画(不走 Viewbox 非等比缩放)：描边恒定细、波峰细腻；曲线下方使用统一蓝色填充。 -->
                                            <Grid x:Name="WaveGraph" ClipToBounds="True">
                                                <Polygon Stroke="{x:Null}" Fill="#4054C7FC">
                                                    <Polygon.Points>
                                                        <MultiBinding Converter="{StaticResource ScalePoints}">
                                                            <Binding Path="HistoryPoints"/>
                                                            <Binding Path="ActualWidth" ElementName="WaveGraph"/>
                                                            <Binding Path="ActualHeight" ElementName="WaveGraph"/>
                                                        </MultiBinding>
                                                    </Polygon.Points>
                                                </Polygon>
                                                <Polyline Stroke="#FF54C7FC" StrokeThickness="1.2" StrokeLineJoin="Round">
                                                    <Polyline.Points>
                                                        <MultiBinding Converter="{StaticResource ScalePoints}">
                                                            <Binding Path="HistoryPoints"/>
                                                            <Binding Path="ActualWidth" ElementName="WaveGraph"/>
                                                            <Binding Path="ActualHeight" ElementName="WaveGraph"/>
                                                        </MultiBinding>
                                                    </Polyline.Points>
                                                </Polyline>
                                            </Grid>
                                            <StackPanel Orientation="Horizontal" Margin="5,3,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
                                                <TextBlock Text="{Binding CoreId}" FontSize="10" Opacity="0.5" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                            </StackPanel>
                                            <TextBlock x:Name="UsageText" Text="{Binding Usage, StringFormat='{}{0:N1}%'}" FontSize="18"  HorizontalAlignment="Center" VerticalAlignment="Center"  Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                            <TextBlock x:Name="OffText" Text="OFF" FontSize="24" FontWeight="Normal" Opacity="0.2" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Visibility="Collapsed"/>
                                        </Grid>
                                    </Border>
                                </Grid>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.SelectedVm.IsRunning, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                        <Setter TargetName="WaveGraph" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="UsageText" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="OffText" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ui:CardExpander>

            <!-- 内存 (Memory) 折叠卡片 -->
            <ui:CardExpander Margin="0,0,0,8" IsExpanded="False">
                <ui:CardExpander.Header>
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xEEA0;" Margin="-5,0,12,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.Label_Memory}" FontSize="16" />
                                <TextBlock Text="{x:Static properties:Resources.Desc_MemoryMonitoring}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- 中间/右侧：数值显示 (A / B 形式) -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{Binding SelectedVm.DemandMemoryGb, StringFormat='{}{0:N1}', FallbackValue='4.2'}" 
                                       FontSize="20" FontWeight="Light" VerticalAlignment="Bottom" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Text="GB" FontSize="14" Margin="2,0,0,3" VerticalAlignment="Bottom" Opacity="0.8"/>

                            <TextBlock Text="/" FontSize="20" FontWeight="Light" VerticalAlignment="Bottom" Margin="8,0,8,0" Opacity="0.4"/>

                            <TextBlock Text="{Binding SelectedVm.AssignedMemoryGb, StringFormat='{}{0:N1}', FallbackValue='16.0'}" 
                                       FontSize="20" FontWeight="Light" VerticalAlignment="Bottom" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Text="GB" FontSize="14" Margin="2,0,0,3" VerticalAlignment="Bottom" Opacity="0.8"/>
                        </StackPanel>

                        <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToMemorySettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18"/>
                        </ui:Button>
                    </Grid>
                </ui:CardExpander.Header>

                <Grid Margin="-5" Height="100">
                    <Border CornerRadius="6" BorderThickness="1" BorderBrush="#1A888888" Background="#05FFFFFF" ClipToBounds="True">
                        <Grid>
                            <Rectangle Fill="{StaticResource FineGridBrush}" Opacity="0.3"/>
                            <Viewbox Stretch="Fill">
                                <Grid Width="59" Height="100">
                                    <Polygon Points="{Binding SelectedVm.MemoryHistoryPoints}" Fill="#FF54C7FC"/>
                                </Grid>
                            </Viewbox>
                            <TextBlock Text="{x:Static properties:Resources.Label_MemoryTrends}" 
                                       FontSize="12" 
                                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
                                       HorizontalAlignment="Left" 
                                       VerticalAlignment="Top" 
                                       Margin="12,8"/>
                        </Grid>
                    </Border>
                </Grid>
            </ui:CardExpander>

            <!-- 存储 (Storage) 折叠卡片 -->
            <ui:CardExpander Margin="0,0,0,8" IsExpanded="False">
                <ui:CardExpander.Header>
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon FontSize="22" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xEDA2;" Margin="-5,0,12,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.Label_Storage}" FontSize="16"  Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="{x:Static properties:Resources.Desc_StorageMonitoring}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- 右侧：总容量 -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{Binding SelectedVm.TotalDiskSizeGb, StringFormat='{}{0:N1}', FallbackValue='0.0'}" 
                                       FontSize="20" FontWeight="Light" VerticalAlignment="Bottom" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Text="GB" FontSize="14" Margin="2,0,0,3" VerticalAlignment="Bottom" Opacity="0.6" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </StackPanel>

                        <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToStorageSettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </ui:Button>
                    </Grid>
                </ui:CardExpander.Header>

                <Grid Margin="-5">
                    <ItemsControl ItemsSource="{Binding SelectedVm.Disks}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,6" CornerRadius="4" BorderThickness="1" BorderBrush="#1A888888" Background="#05FFFFFF">
                                    <Grid Margin="12,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <ui:FontIcon Grid.Column="0" 
                                                     FontFamily="{DynamicResource SegoeFluentIcons}"
                                                     FontSize="28" 
                                                     VerticalAlignment="Center" 
                                                     Margin="0,0,16,0" 
                                                     Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
                                            <ui:FontIcon.Style>
                                                <Style TargetType="ui:FontIcon">
                                                    <Setter Property="Glyph" Value="&#xEC59;"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DiskType}" Value="ISO">
                                                            <Setter Property="Glyph" Value="&#xE958;"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DiskType}" Value="DvdDrive">
                                                            <Setter Property="Glyph" Value="&#xE958;"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DiskType}" Value="DVD">
                                                            <Setter Property="Glyph" Value="&#xE958;"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:FontIcon.Style>
                                        </ui:FontIcon>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
               Text="{Binding Name}" 
               FontSize="14" 
               TextTrimming="CharacterEllipsis"
               VerticalAlignment="Center"
               Margin="0,0,12,0"
               Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>

                                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding IoSpeedText}" 
                   FontSize="11" 
                   Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
                   Opacity="0.8"
                   Margin="0,0,12,0"
                   VerticalAlignment="Center"
                   FontFamily="Segoe UI, Microsoft YaHei">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding DataContext.SelectedVm.IsRunning, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>

                                                    <Border Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" 
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="3" 
                Padding="5,1">
                                                        <TextBlock Text="{Binding DiskType, Converter={StaticResource DiskTypeToTextConverter}}" 
                       FontSize="12" 
                       Opacity="0.8" 
                       FontWeight="Medium"
                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                                    </Border>
                                                </StackPanel>
                                            </Grid>
                                            <Grid Margin="0,8,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <ProgressBar Grid.Column="0" 
                                                             Value="{Binding UsagePercentage, Mode=OneWay}" 
                                                             Maximum="100" 
                                                             Height="12" 
                                                             VerticalAlignment="Center" 
                                                             Background="#1A888888" 
                                                             Foreground="#FF54C7FC"/>
                                                <TextBlock Grid.Column="1" 
                                                           Text="{Binding UsageText}" 
                                                           FontSize="12" 
                                                           Margin="12,0,0,0" 
                                                           VerticalAlignment="Center"
                                                           Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                            </Grid>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="{x:Static properties:Resources.Warn_NoVhd}" HorizontalAlignment="Center" Opacity="0.4" Margin="20" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedVm.Disks.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </ui:CardExpander>

            <!-- 显卡 (GPU) 折叠卡片 -->
            <ui:CardExpander Margin="0,0,0,8" IsExpanded="False">
                <ui:CardExpander.Header>
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon FontSize="22" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xF211;" Margin="-5,0,12,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.Label_Gpu}" FontSize="16" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="{x:Static properties:Resources.Desc_GpuMonitoring}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- 中间：总体利用率数值 -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontSize="20" FontWeight="Light" VerticalAlignment="Bottom" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding SelectedVm.GpuMaxUsage, StringFormat='{}{0:N1}'}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedVm.HasGpu}" Value="False">
                                                <Setter Property="Text" Value="0.0"/>
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding SelectedVm.IsGpuActive}" Value="False">
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="%" FontSize="14" Margin="2,0,0,3" VerticalAlignment="Bottom" Opacity="0.8"/>
                        </StackPanel>

                        <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToGpuSettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18"/>
                        </ui:Button>
                    </Grid>
                </ui:CardExpander.Header>

                <Grid Margin="-5">
                    <UniformGrid Rows="1" Columns="4">
                        <UniformGrid.Style>
                            <Style TargetType="UniformGrid">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding SelectedVm.IsRunning}" Value="True"/>
                                            <Condition Binding="{Binding SelectedVm.IsGpuActive}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </UniformGrid.Style>

                        <Grid Margin="2" Height="80">
                            <Border CornerRadius="4" BorderThickness="1" BorderBrush="#1A888888" Background="#08FFFFFF" ClipToBounds="True">
                                <Grid>
                                    <Rectangle Fill="{StaticResource FineGridBrush}"/>
                                    <Viewbox Stretch="Fill">
                                        <Grid Width="100" Height="100">
                                            <Polygon Points="{Binding SelectedVm.Gpu3dHistoryPoints}" Fill="#66117DBB"/>
                                            <Polyline Points="{Binding SelectedVm.Gpu3dHistoryPoints}" Stroke="#FF54C7FC" StrokeThickness="1.5"/>
                                        </Grid>
                                    </Viewbox>
                                    <TextBlock Text="{x:Static properties:Resources.Label_3DRender}" FontSize="10" Margin="5,3" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                    <TextBlock Text="{Binding SelectedVm.Gpu3dUsage, StringFormat='{}{0:N1}%'}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"  Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <Grid Margin="2" Height="80">
                            <Border CornerRadius="4" BorderThickness="1" BorderBrush="#1A888888" Background="#08FFFFFF" ClipToBounds="True">
                                <Grid>
                                    <Rectangle Fill="{StaticResource FineGridBrush}"/>
                                    <Viewbox Stretch="Fill">
                                        <Grid Width="100" Height="100">
                                            <Polygon Points="{Binding SelectedVm.GpuCopyHistoryPoints}" Fill="#66117DBB"/>
                                            <Polyline Points="{Binding SelectedVm.GpuCopyHistoryPoints}" Stroke="#FF54C7FC" StrokeThickness="1.5"/>
                                        </Grid>
                                    </Viewbox>
                                    <TextBlock Text="{x:Static properties:Resources.Label_CopyData}" FontSize="10" Margin="5,3" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                    <TextBlock Text="{Binding SelectedVm.GpuCopyUsage, StringFormat='{}{0:N1}%'}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"  Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <Grid Margin="2" Height="80">
                            <Border CornerRadius="4" BorderThickness="1" BorderBrush="#1A888888" Background="#08FFFFFF" ClipToBounds="True">
                                <Grid>
                                    <Rectangle Fill="{StaticResource FineGridBrush}"/>
                                    <Viewbox Stretch="Fill">
                                        <Grid Width="100" Height="100">
                                            <Polygon Points="{Binding SelectedVm.GpuEncodeHistoryPoints}" Fill="#66117DBB"/>
                                            <Polyline Points="{Binding SelectedVm.GpuEncodeHistoryPoints}" Stroke="#FF54C7FC" StrokeThickness="1.5"/>
                                        </Grid>
                                    </Viewbox>
                                    <TextBlock Text="{x:Static properties:Resources.Label_EncVideo}" FontSize="10" Margin="5,3" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                    <TextBlock Text="{Binding SelectedVm.GpuEncodeUsage, StringFormat='{}{0:N1}%'}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"  Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <Grid Margin="2" Height="80">
                            <Border CornerRadius="4" BorderThickness="1" BorderBrush="#1A888888" Background="#08FFFFFF" ClipToBounds="True">
                                <Grid>
                                    <Rectangle Fill="{StaticResource FineGridBrush}"/>
                                    <Viewbox Stretch="Fill">
                                        <Grid Width="100" Height="100">
                                            <Polygon Points="{Binding SelectedVm.GpuDecodeHistoryPoints}" Fill="#66117DBB"/>
                                            <Polyline Points="{Binding SelectedVm.GpuDecodeHistoryPoints}" Stroke="#FF54C7FC" StrokeThickness="1.5"/>
                                        </Grid>
                                    </Viewbox>
                                    <TextBlock Text="{x:Static properties:Resources.Label_DecVideo}" FontSize="10" Margin="5,3" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                    <TextBlock Text="{Binding SelectedVm.GpuDecodeUsage, StringFormat='{}{0:N1}%'}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"  Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </UniformGrid>

                    <Border Padding="30">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding SelectedVm.HasGpu}" Value="True"/>
                                            <Condition Binding="{Binding SelectedVm.IsRunning}" Value="True"/>
                                            <Condition Binding="{Binding SelectedVm.IsGpuActive}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="{x:Static properties:Resources.Warn_GpuNoDriver}" HorizontalAlignment="Center" Opacity="0.6" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <Border Padding="30">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding SelectedVm.HasGpu}" Value="True"/>
                                            <Condition Binding="{Binding SelectedVm.IsRunning}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{x:Static properties:Resources.Warn_VmNotStarted}" HorizontalAlignment="Center" Opacity="0.4" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                    </Border>

                    <Border Padding="30">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedVm.HasGpu}" Value="False">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{x:Static properties:Resources.Warn_NoGpu}" HorizontalAlignment="Center" Opacity="0.4" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                    </Border>
                </Grid>
            </ui:CardExpander>

            <!-- 网络 (Network) 折叠卡片 -->
            <ui:CardExpander Margin="0,0,0,8" IsExpanded="False">
                <ui:CardExpander.Header>
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon FontSize="22" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xE774;" Margin="-5,0,12,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.Label_Net}" FontSize="16" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="{x:Static properties:Resources.Desc_NetInfo}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToNetworkSettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </ui:Button>
                    </Grid>
                </ui:CardExpander.Header>

                <Grid Margin="-5">
                    <ItemsControl ItemsSource="{Binding SelectedVm.NetworkAdapters}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,6" CornerRadius="4" BorderThickness="1" BorderBrush="#1A888888" Background="#05FFFFFF">
                                    <Grid Margin="12,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <ui:FontIcon Grid.Column="0" 
                                                     FontFamily="{DynamicResource SegoeFluentIcons}"
                                                     FontSize="28" 
                                                     Glyph="&#xE839;"
                                                     VerticalAlignment="Center" 
                                                     Margin="0,0,16,0">
                                            <ui:FontIcon.Style>
                                                <Style TargetType="ui:FontIcon">
                                                    <Setter Property="Foreground" Value="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                                            <Setter Property="Foreground" Value="{ui:ThemeResource TextFillColorDisabledBrush}"/>
                                                            <Setter Property="Opacity" Value="0.5"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:FontIcon.Style>
                                        </ui:FontIcon>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding IpAddressDisplay, FallbackValue={x:Static properties:Resources.VmDashboard_TabBoot}}"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,12,0"
                                                           Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>

                                                <StackPanel Grid.Column="1"
                                                            Orientation="Horizontal"
                                                            HorizontalAlignment="Right"
                                                            VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding NetworkSpeedText}"
                                                               FontSize="11"
                                                               Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
                                                               Opacity="0.8"
                                                               Margin="0,0,12,0"
                                                               VerticalAlignment="Center"
                                                               FontFamily="Segoe UI, Microsoft YaHei"/>
                                                    <Border Background="{ui:ThemeResource ControlFillColorTertiaryBrush}"
                                                            BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="3"
                                                            Padding="5,1">
                                                        <TextBlock Text="{Binding SwitchName, FallbackValue='{x:Static properties:Resources.Status_Unconnected}'}"
                                                                   FontSize="12"
                                                                   Opacity="0.8"
                                                                   FontWeight="Medium"
                                                                   Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                                    </Border>
                                                </StackPanel>
                                            </Grid>

                                            <TextBlock Text="{Binding MacAddress}" 
                                                       FontSize="10" 
                                                       
                                                       Margin="0,4,0,0"
                                                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="{x:Static properties:Resources.Warn_NoAdapter}" HorizontalAlignment="Center" Opacity="0.4" Margin="20" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedVm.NetworkAdapters.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </ui:CardExpander>

            <ui:Card Margin="0,0,0,8" Padding="14,16,14,16">
                <Grid MinHeight="38" Margin="8,0,12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <ui:FontIcon FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xF0B9;" Margin="-5,0,12,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{x:Static properties:Resources.VmPcie_Title}" FontSize="16" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Text="{x:Static properties:Resources.VmPcie_Hint}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </StackPanel>
                    </StackPanel>

                    <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToPcieSettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <ui:SymbolIcon Symbol="Settings24" FontSize="18" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                    </ui:Button>
                    <ui:SymbolIcon Grid.Column="5" Symbol="ChevronDown24" FontSize="16" Visibility="Hidden" VerticalAlignment="Center"/>
                </Grid>
            </ui:Card>

            <!-- 引导顺序 (Boot Order) 卡片 -->
            <ui:Card Margin="0,0,0,8" Padding="14,16,14,16">
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon FontSize="22" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xF2C7;" Margin="-5,0,12,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.VmDashboard_DescBoot}" FontSize="16" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="{x:Static properties:Resources.VmDashboard_TabSpacetime}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <ui:Button Grid.Column="2" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToBootSettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </ui:Button>
                        <!-- 占位，撑出 chevron 宽度让齿轮对齐 -->
                        <ui:SymbolIcon Grid.Column="3" Symbol="ChevronDown24" FontSize="16" Visibility="Hidden" VerticalAlignment="Center"/>
                    </Grid>
            </ui:Card>

            <!-- 时空 (Spacetime / Snapshots) 卡片 -->
            <ui:Card Margin="0,0,0,8" Padding="14,16,14,16">
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xF738;" Margin="-5,0,12,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.VmDashboard_DescSpacetime}" FontSize="16" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="{x:Static properties:Resources.VmDashboardView_SnapshotAndSpacetime}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToSpacetimeSettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </ui:Button>
                        <!-- 占位，撑出 chevron 宽度让齿轮对齐 -->
                        <ui:SymbolIcon Grid.Column="5" Symbol="ChevronDown24" FontSize="16" Visibility="Hidden" VerticalAlignment="Center"/>
                    </Grid>
            </ui:Card>

            <!-- 高级 (Advanced) 卡片 -->
            <ui:Card Margin="0,0,0,8" Padding="14,16,14,16">
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xE713;" Margin="-5,0,12,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.VmDashboard_DescAdvanced}" FontSize="16" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="{x:Static properties:Resources.VmDashboard_HintAdvanced}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToAdvancedSettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </ui:Button>
                        <!-- 占位，撑出 chevron 宽度让齿轮对齐 -->
                        <ui:SymbolIcon Grid.Column="5" Symbol="ChevronDown24" FontSize="16" Visibility="Hidden" VerticalAlignment="Center"/>
                    </Grid>
            </ui:Card>

            <!-- 安全 (Security) 卡片 -->
            <ui:Card Margin="0,0,0,8" Padding="14,16,14,16">
                    <Grid MinHeight="38" Margin="8,0,12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="ShieldCheckmark24" FontSize="22" Margin="-2,0,12,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{x:Static properties:Resources.VmSecurity_Title}" FontSize="16" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="{x:Static properties:Resources.VmSecurity_Hint}" FontSize="12" Opacity="0.6" Margin="0,2,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>

                        <ui:Button Grid.Column="4" Appearance="Transparent" Padding="8,4" Command="{Binding DataContext.GoToSecuritySettingsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </ui:Button>
                        <!-- 占位，撑出 chevron 宽度让齿轮对齐 -->
                        <ui:SymbolIcon Grid.Column="5" Symbol="ChevronDown24" FontSize="16" Visibility="Hidden" VerticalAlignment="Center"/>
                    </Grid>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
